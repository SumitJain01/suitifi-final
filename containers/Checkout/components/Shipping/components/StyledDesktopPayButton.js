import Button from 'shared/components/Button';
import styled from 'styled-components';

export default styled(Button)`
    &.mobileHidden.payNowBtn span {
        font-size: 1.2em;
        font-weight: 700;
        line-height: 0.9em;
    }
`;