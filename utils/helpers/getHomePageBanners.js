import { CONTENTFUL_SPACE_ID, CONTENTFUL_ACCESS_TOKEN, HOMEPAGE_CONTENT_ID } from 'utils/constants/index';
import request from 'lib/request';
import { isEmpty } from 'ramda';

export default async function getHomePageBanners(){
  let desktopSliderImages = [], mobileSliderImages = [];
  
    return (
      {desktopSliderImages, mobileSliderImages}
    )
  }
